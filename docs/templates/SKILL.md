---
name: <name>
description: Use when <trigger conditions - the situations and phrasings that should make Claude reach for this skill>. Do NOT use for <the nearest thing it is not>.
---

# <Name>

One paragraph: what this skill gives Claude that it does not have by default.

## Prerequisites

What must exist before this works - installed binary, environment variable, login.
Say how to check, not just what to install.

## Usage

Concrete commands or steps, in the order they are run.

```bash
<command>
```

## Gotchas

The things that cost an hour the first time. Rate limits, locale quirks, flags that look
optional but are not.

## References

- `references/<file>.md` - loaded on demand, keep the deep detail here rather than above
